# Cursor mirror bus

Cloud agent writes `pending.json`. Mac worker reads, executes, writes `done.json`.  
**Not** full chat clone — intent + results only.

See `docs/AGENT_KIT.md` and `docs/MOBILE_CONTROL.md`.
