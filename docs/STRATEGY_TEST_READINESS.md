# 3-MONTH STRATEGY TEST — READINESS (checked June 12, keys dry)
*Pre-build truth, before we run the CEO→creative→produce test. "Is this actually true?"*

## VERDICT: not ready to run — blocked on jurisha's strategic truth (the fix is already staged)

## The knowledge side (the Brain the CEO queries)
- **Supabase: NOT connected** — DATABASE_URL is unset. The sync layer isn't live right now.
- **BUT the knowledge exists locally**: 110 benchmark accounts + intelligence_layer.json (237KB
  of patterns). The CEO can build strategy from THIS today; Supabase is the scale path, not a
  blocker. → Decision needed: wire Supabase, or CEO reads the local intelligence layer for v1.

## The client side (jurisha's strategic truth) — THE REAL BLOCKER
The CEO cannot build a real 3-month strategy because her strategic organs are empty:
- **goals: NOT declared** (goal_ratio null, capacity null, USP-in-her-words null)
- **red_lines: empty**
- **fingerprint l1_strategy + l2_voice: empty**
- audience: maps_signals present ✓ (997 reviews), customer_language thin (5)
- products: 2 real ✓

**These gaps ARE the 6 strategy questions — now Amira's lane on the team link.** Until she (or
Mohamed) answers them, the CEO would INVENT the strategy — the exact thing Mohamed forbade
("strategy from data, not air").

## THE PATH (clean sequence)
1. **Amira answers the 6 strategy questions** (her link is built) → fills goals/red_lines/voice
2. **Decide the knowledge source** — wire Supabase, or CEO uses local intelligence_layer for v1
3. **Keys refill** → CEO builds strategy from real truth + real knowledge → creative → produce
   3 months → team judges by lane
4. The walking skeleton (1 post, full trace) runs first to prove the path, then scale to 90 days

## Bottom line
The team link wasn't just the right first build — it's the *unblocker*. Amira filling jurisha's
6 answers is now the critical path to the whole 3-month test. Everything else waits on keys.
