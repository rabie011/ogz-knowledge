# Archived 2026-06-11 (P4 hygiene)
Spent one-shot scripts (write_*/print_*/test_*/fix_*/backfill_*). Moved, never deleted (house rule). The ~25 live pipeline scripts stay in scripts/. Nothing here is imported by a live path (verified before move).
